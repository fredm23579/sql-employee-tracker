const pool = require('./connection');

// Define functions to perform SQL queries
// Example:
// async function getAllDepartments() {
//     const [rows, fields] = await pool.query('SELECT * FROM department');
//     return rows;
// }

module.exports = {
    // Define your SQL query functions here
};
